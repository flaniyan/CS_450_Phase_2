name: Copilot PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:  # allows manual run via CLI or Actions tab

permissions:
  contents: read
  pull-requests: write  # required for leaving PR review comments

jobs:
  copilot-pr-review:
    name: Run Copilot PR Review
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Copilot review
        uses: actions/github-script@v7
        with:
          script: |
            console.log('Copilot PR review placeholder - no official action available');
