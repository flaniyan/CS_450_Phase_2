services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - PORT=3000
    volumes:
      # Mount for development (hot reload)
      - ./src:/app/src
      - ./frontend:/app/frontend
    restart: unless-stopped